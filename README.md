Am atasat si un import de request-uri.
La inceput am vrut sa fac doar din endpoint-uri dar am zis ca ma complic.
Customer-ul trebuie adaugat din endpoint (am in importul de requesturi).
Apoi se face requestul initialize care imi face 10 produse hardcodate si cate un OrderDetail pentru fiecare.
Le adauga apoi la un Order si apoi adauga Order-ul la customerul cu id 1
Se pot rula dupa request-urile de getAllProducts, getOrderByCustomerId si getPostalDetails care fac ce scria in cerinta.
Am mai adaugat endpoint-uri pentru update, delete, get customer si pentru add porduct, dar am renuntat sa fac totul din endpoint-uri pentru ca lua mult timp.
